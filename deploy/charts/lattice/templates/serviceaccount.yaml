apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "lattice.serviceAccountName" . }}
  labels:
    {{- include "lattice.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
